server {
  listen 80;
  server_name _;
  root /usr/share/nginx/html;
  index index.html;

  location = /metrics {
    default_type text/plain;
    return 200 'frontend_build_info{version="19.2",service="cns-crm"} 1\nfrontend_up 1\n';
  }

  location / {
    try_files $uri /index.html;
  }
}
